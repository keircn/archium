Examples

This directory contains example plugins you can build and drop into your
plugin directory (usually `~/.config/archium/plugins`).

Build examples:

```bash
cd examples/hello
make

cd ../hooks
make
```

Copy the resulting `.so` files into your plugin directory and restart Archium.
